<!doctype html>
<html lang="en">
<head>
    <title></title>
    <style>
        body {
            font-family: "-apple-system", "system-ui", "Arial", sans-serif;
        }
    </style>
</head>
<body>
    <p><b>Current time: {{ now.astimezone() }}</b></p>
    {% for entry in storage %}
        <p>{{ entry.date }}</p>
        <ul>
            <li>Price: {{ entry.price }}</li>
            <li>Product IDs: {{ entry.product_ids }}</li>
        </ul>
    {% endfor %}
</body>
</html>